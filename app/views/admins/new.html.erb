<!DOCTYPE html>
<html lang="en">
<head>
  <title>Sign Up Administrador</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <%= javascript_include_tag "bootstrapValidator" %>

  <style>
    /* Set height of the grid so .sidenav can be 100% (adjust as needed) */
    .row.content {height: 720px}

    label {
      padding-top: 5px;
    }
    h2 {
      margin: 0;
    }

    hr {
      margin-top: 10px;
      margin-bottom: 10px;
    }

    .alert {
      margin-top: 5px;
      display:inline-block;
      vertical-align: middle;
      height: 50%;
      padding: 1px;
      margin-top: 1%;
      margin-bottom: 1%;
    }
  </style>

</head>
<body>

<hr>
<h2><strong>Sign Up</strong></h2>
<hr>

<%= form_with(url: admins_new_url, local: true) do |f| %>

  <div class="row">
    <div class="col-md-4"></div>
    <div class="well form-horizontal col-md-4">

      <div class="row">
        <p class="col-md-3 text-left">
          <strong>Usuario </strong>
        </p>
        <p class="col-md-9 text-left">
          <%= f.text_field :username, {id: :admin_username, size: 20}%>
        </p>
      </div>

      <div class="row">
        <p class="col-md-3 text-left">
          <strong>Correo </strong>
        </p>
        <p class="col-md-9 text-left">
          <%= f.text_field :email, id: :admin_email, size: 20%>
        </p>
      </div>

      <div class="row">
        <p class="col-md-3 text-left">
          <strong>Contraseña </strong>
        </p>
        <p class="col-md-9 text-left">
          <%= f.password_field :password, id: :admin_password, size: 20%>
        </p>
      </div>

      <div class="row">
        <p class="col-md-3 text-left">
          <strong>Confirmar Contraseña </strong>
        </p>
        <p class="col-md-9 text-left">
          <%= f.password_field :password_confirmation, id: :admin_password_confirmation, size: 20%>
        </p>
      </div>

      <% if @admin && @admin.errors.any? %>
        <div class="row">
          <p class="col-md-12 alert alert-danger">
            <strong><%= alert %></strong>
            <% @admin.errors.full_messages.each do |e| %>
              <li class="alert alert-danger pull-left"><%= " "+e+" " %></li>
            <% end %>
          </p>
        </div>
      <% elsif notice %>
        <div class="row">
          <p class="col-md-12 alert alert-success">
            <strong><%= notice %></strong>
          </p>
        </div>
      <% end %>
      </div>
      <div class="col-md-4"></div>
    </div>
    <br>
    <div class="actions">
      <%= f.submit 'Signup', class: "btn btn-primary btn-md" %>
    </div>
  <% end %>

</body>
</html>
