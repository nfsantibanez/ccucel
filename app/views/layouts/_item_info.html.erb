<!DOCTYPE html>
<html lang="en">
<head>
  <title>Información de Usuario</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
  </style>

  <script type="text/javascript">
  $(document).ready(function(){

  });
  </script>


</head>
<body>
<!-- Informacion del dispositivo del usuario -->
  <% if @user["item"] == 'smartphone' && @items.has_key?(:sp)%>
    <%= render partial: '/layouts/item_sp', locals: {form: form} %>
  <% elsif @user["item"] == 'bam' && @items.has_key?(:bam)%>
    <%= render partial: '/layouts/item_bam', locals: {form: form} %>
  <% elsif @user["item"] == 'sim' && @items.has_key?(:sim)%>
    <%= render partial: '/layouts/item_sim', locals: {form: form} %>
  <% end %>
</body>
</html>
